<form action="{{ path('liip_translation_import_file') }}" method="POST" {{ form_enctype(form) }} class="form-inline" role="form">
    {{ form_rest(form) }}
    <input type="submit" class="btn btn-default" value="{{ 'button.import' | trans({}, 'translation-bundle') }}">
</form>